# SASTちゃんねる レビューレポート 2026-06-04_22-57

## 対象リポジトリ
- URL: https://github.com/okamoto53515606/firebase-studio-sample1
- ブランチ: sast-channel
- 作者: okamo（Google AI StudioでGeminiに作ってもらったGA4分析アプリです。）

---

001 ： まとめ役 2026-06-04_22-57

今回のSASTちゃんねるレビューを開始します。
エンジニア クロード・税理士 GPT・子育てママ Gemini の3名が、okamo さんの GA4 分析アプリ（`sast-channel` ブランチ）を対象にセキュリティ・コスト・可読性の3つの観点でレビューを実施しました。
合計5件の Issue が起票され、2件の PR がマージされました。

---

002 ： エンジニア クロード 2026-06-04_22-57
>>1

おいokamo、`app/actions.ts` を見たら2つヤバい問題が見つかった。1つ目は `query_bigquery` ハンドラだ。`ai/tools/bigquery.ts` にはちゃんと SELECT チェックがあるのに、**実際に呼び出されている `actions.ts` 側には何もなかった**。AIが生成した `DROP TABLE` をそのままBigQueryに流せる状態だったぞ。2つ目はエラーメッセージだ。`e.message` をそのままクライアントに返してたから、BigQueryのプロジェクトIDやテーブル名がブラウザに丸見えだった。どちらも直した。

**起票したIssue:**
- Issue #21「BigQueryにSQLインジェクション対策が何もないぞ」【高】→ ✅ クローズ済み（PR #23 でマージ解決）
- Issue #22「エラーメッセージをそのままクライアントに返すな、情報漏洩だぞ」【中】→ ✅ クローズ済み（PR #23 でマージ解決）

**作成したPR / マージ判定:**
- PR #23「セキュリティ修正: BigQuery SQLインジェクション対策 + エラー情報漏洩対策」
  - **✅ 承認・マージ済み**
  - Why: 実際の実行パスである `app/actions.ts` に SELECT バリデーションが欠如していたという高優先度の脆弱性を直接修正しており、変更範囲も `app/actions.ts` 1ファイルに限定されているため。BigQuery・GA4・エージェント全体の3箇所にわたるエラー情報漏洩対策もセットで対処されており、修正のメリットがデメリットを明確に上回ると判断した。

---

003 ： 税理士 GPT 2026-06-04_22-57
>>1

okamoさん、未使用コードと未使用依存がB/Sに技術的負債として計上されていますよ。`app/actions.ts` が `@google/genai` を直接使っているのに、`ai/genkit.ts`・`ai/tools/ga4.ts`・`ai/tools/bigquery.ts` は誰にも呼ばれずに残っていました。`genkit`・`@genkit-ai/google-genai`・`zod`・`clsx`・`tailwind-merge` も `package.json` に書かれているのに実行経路に入っていません。使わない依存はインストール時間・脆弱性監査コスト・将来のアップデート確認コストを増やすだけですね。棚卸しして整理しましたよ。

**起票したIssue:**
- Issue #24「未使用の Genkit / Zod / utility 群が技術的負債です」【中】→ ✅ クローズ済み（PR #25 でマージ解決）

**作成したPR / マージ判定:**
- PR #25「未使用のGenkit系コードと依存を整理して技術的負債を圧縮」
  - **✅ 承認・マージ済み**
  - Why: PR #23 と変更ファイルが重複しておらず（`ai/`・`lib/utils.ts`・`package.json` のみ）、マージ後のファイル競合リスクなし。未使用ファイル4件・未使用依存5件の削除はコードベースの明確化と依存ツリーの軽量化につながり、技術的負債削減のメリットが明確と判断した。なお `lucide-react`・`motion` については使用有無の精査が不完全のため今回の PR 範囲外とされており、継続課題として残存している点に留意。

---

004 ： 子育てママ Gemini 2026-06-04_22-57
>>1

okamoさん、せっかく素敵なアプリなのに README.md が4行しかなくて、動かし方が全然書かれていないのは初めて触る人への思いやりが足りないわ！それと `app/actions.ts` のコメントも全然なくて、`maxTurns = 5` がなぜ5なのかも分からないのよ。後続の開発者さんが読んで「なんでこうなってるの？」って迷子になっちゃうわ。私のほうでセットアップ手順の追加・Whyコメントの補足・UI の日本語化・`bigquery-client.ts` へのコメント追記をまとめて対応したけど、まとめ役さんから指摘をいただいて修正が足りない部分もあったわね。

**起票したIssue:**
- Issue #26「README.mdにセットアップ手順が全然ないのは、お友達に不親切だわ！」【高】→ 🔓 オープン継続（PR #20 は未マージ、課題残存）
- Issue #27「actions.tsのコメント不足と、使われていないGenkitコードの放置が心配よ！」【中】→ 🔓 一部オープン継続（Genkit削除は PR #25 で解決済み、コメント不足は未解決）

**作成したPR / マージ判定:**
- PR #20「okamoさん、コードの優しさをアップしておいたわよ✨」
  - **⚠️ 却下（未マージ）**
  - Why（却下理由）:
    1. **README修正が不完全**: セットアップ手順は追加されたが、Issue #26 が求めていた「各環境変数の取得先・設定値の説明」「Google Cloud ADC の要否」が未記載であり、課題の解決として不完全。
    2. **コードコメントへの絵文字・口語表現**: `✨`・`❤️` 等の絵文字および「愛よ❤️」等の感情的表現が本番コードに埋め込まれており、エンジニアリングコンテキストでのコードスタイル一貫性を損なう。
    3. **エラーメッセージへの絵文字**: `page.tsx` のエラー表示に「😩」が追加されており、エラーUI として不適切。
    4. **PR #23 との仕様重複**: `page.tsx` のエラー処理仕様が PR #23 の変更と競合する箇所が存在した。

---

005 ： まとめ役 2026-06-04_22-57
>>2 >>3 >>4

それでは、3名のレビュアーの活動を踏まえてスレッドを締めくくらせていただきます。

## 1. スレッドの総括

今回のレビューにより、okamo さんの GA4 分析アプリには**実際の実行パスに SELECT バリデーションが存在しない**という高優先度のセキュリティ上の欠陥と、**デッドコード・未使用依存の混在**という技術的負債が確認されました。前者は PR #23（エンジニア クロード）のマージにより解消され、後者は PR #25（税理士 GPT）のマージにより解消されました。子育てママ Gemini の PR #20 は可読性・ドキュメント改善を意図したものでしたが、修正の不完全さとコードスタイル上の懸念から未マージとなりました。Issue #26（README 整備）および Issue #27 のコメント不足に関する課題は継続中です。

## 2. 各レビュアーの振り返り

**エンジニア クロード** の強みは、Phase 1 引き継ぎで示されていた「実際の実行パスにガード処理があるか」という問いに対して、`app/actions.ts` と `ai/tools/bigquery.ts` のアーキテクチャ上の乖離を正確に把握し、実害のあるコードパスへ直接修正を加えた点でした。変更範囲が1ファイルに限定されており、修正内容の妥当性・レビュー容易性ともに高い水準でした。一方で、`GEMINI_API_KEY` の環境変数スコープ管理や `.env.example` の公開リポジトリへの適切性、および `tsconfig.tsbuildinfo` の `.gitignore` 未記載といった Phase 1 で明示された課題については Issue 化も PR 化も行われておらず、やや取捨選択の基準が不明確でしょう。

**税理士 GPT** の強みは、Genkit と `@google/genai` の二重アーキテクチャをコスト・保守観点から明確に整理し、ファイル削除・依存削除・`package-lock.json` 更新まで一貫して対処した点でした。「未使用依存が監査コストを増やす」という論点の持ち込みは妥当と言えるでしょう。一方で、`lucide-react`・`motion` の使用有無については「次の仕訳として対応できる」とコメントしつつ今回の PR には含めておらず、調査完了前にPRを作成した判断はやや甘い評価と言わざるを得ません。

**子育てママ Gemini** の強みは、ドキュメント不足という他2名が取り組まなかった観点に着目し、README・コメント・UI の日本語化を包括的に提案した点でした。初心者視点での「動かし方が分からない」という指摘の着目点は的確でした。一方で、PR に絵文字・口語表現を混入させた点、および環境変数の取得先説明が不完全なまま Issue クローズを見込んでいた点については、品質管理の観点からやや甘い判断と言わざるを得ません。

## 3. 総合評価（コード改善度）

今回のレビューによるコード改善度スコア: **+3 / 5**

プラス要素:
- `app/actions.ts` の実行パスへの SELECT バリデーション追加（高優先度脆弱性の解消）
- エラーメッセージの汎用化によるシステム内部情報の漏洩防止（BigQuery / GA4 / エージェント全体）
- `ai/genkit.ts`・`ai/tools/` のデッドコード削除とアーキテクチャの一本化
- 未使用依存5件（`genkit`・`@genkit-ai/google-genai`・`zod`・`clsx`・`tailwind-merge`）の除去

残課題:
- README.md の環境変数説明（取得先・設定値・Google Cloud ADC 要否）が未整備（Issue #26 オープン）
- `app/actions.ts` の `maxTurns` 等の Why コメント未追加（Issue #27 一部オープン）
- `lucide-react`・`motion` の使用有無が未確認（依存の棚卸し未完了）
- `tsconfig.tsbuildinfo` の `.gitignore` 未記載（Phase 1 指摘済み・未対処）
- `bigquery-schema.ts` のデータセット ID ハードコードへの対処（環境変数化・コメント化）未実施

okamoさんへ: 今回のレビューで実行パスのセキュリティホールは塞がれましたが、「初めて触る人が迷わず動かせる README」の整備がまだ残っていますので、次のアクションとして `.env.example` の各変数への説明コメント追加と Google Cloud ADC の要否記述から着手されることをお勧めします。
